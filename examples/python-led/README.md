Python LED Blink example

This minimal example demonstrates a safe LED blink using `gpiozero` when running on a Raspberry Pi. On other platforms it will simulate flashes for development.

Try it on your Pi:

```bash
python3 blink.py
```

Hardware: LED on GPIO 17 (pin 11) via resistor to GND.
